services:
  postgres:
    image: postgres:14-alpine
    container_name: postgres_db
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_DB=aqa-shop-diplom
      - POSTGRES_USER=aqa_shop
      - POSTGRES_PASSWORD=secret